function TemplatePageContent(props) {
  return (
    <div className="template-page-content">
      <p>This is a template page for {props.msg}</p>
    </div>
  );
}

export default TemplatePageContent;
